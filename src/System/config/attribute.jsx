export const attribute_dinamic = (key, type, param, label) => {
  return `<font> </font><sup><font key="${key}" type="${type}" params="${param}" style="background:green; color:#fff;border-radius:5px">${label}</font></sup><font> </font> &emsp;`;
};
